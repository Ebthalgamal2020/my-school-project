# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log

# If you ever add tooling
node_modules/
dist/
.env
.env.local

# Claude Code local state
.claude/

# Raw video. GitHub rejects any file over 100MB, and a background video
# should ship at 2-5MB anyway. Compress before committing, then remove
# this line for the compressed file only.
deo-background.mp4
*.mov
*.avi

# Saved copy of the real deokairo.com WordPress site (7.8MB, ~29 vendor
# stylesheets). It's reference material, not project code — and republishing
# another site's theme, scripts and images from a public repo isn't ours to do.
Homepage _ DEO Cairo_files/
Homepage _ DEO Cairo.html
Homepage _ DEO Cairo.png
